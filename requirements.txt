# COMC arbitrage scanner — runtime dependencies
#
# Core engine (TCGCSV pricing, matching, ranking, reporting) needs only `requests`
# plus the stdlib. The extras below improve matching/parsing and enable COMC scraping.

requests>=2.31              # HTTP client for the TCGCSV price API
rapidfuzz>=3.9              # fast fuzzy name matching (falls back to stdlib difflib if absent)

# --- COMC scraping (only needed for live COMC runs; lazy-imported) ---
playwright>=1.44            # real Chromium to clear COMC's Cloudflare challenge
playwright-stealth>=1.0.6   # reduce headless fingerprinting (optional)
selectolax>=0.3            # fast HTML parsing (falls back to stdlib html.parser if absent)

# --- Optional Google Sheets push (lazy-imported; CSV is written regardless) ---
gspread>=6.0
google-auth>=2.29

# Note: httpx, pandas, tabulate, rich, tenacity and python-dotenv are intentionally
# NOT required — the code uses the stdlib (csv/json/urllib-free requests/difflib) and a
# built-in .env loader so the core runs in minimal environments.
